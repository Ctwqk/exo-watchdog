# exo-watchdog

Control-plane service for Exo routing, provider selection, and model governance.

Tracked here:
- `app.py`
- container and local runtime files
- placeholder directories for runtime state

Not tracked here:
- SQLite data under `data/`
- SSH runtime state under `ssh/` and `ssh-user/`
- Python cache files
